{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -n \"$GITLAB_TOKEN\" || printf '%s' 'The wanadev-gitlab GitLab MCP plugin has no GITLAB_TOKEN set. Proactively tell the user (in their language): 1) GitLab > Settings > Access Tokens, 2) create a token with the api scope, 3) set it as the GITLAB_TOKEN environment variable. Be friendly and concise.'"
          }
        ]
      }
    ]
  }
}
